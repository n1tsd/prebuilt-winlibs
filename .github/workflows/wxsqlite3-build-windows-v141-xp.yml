# Build wxSQLite3 for Win32 and x64 with MSVC platform toolset v141_xp. wxWidgets uses the same
# toolset. Runner: windows-latest (no reliance on deprecated images); VS 2022 Build Tools is
# installed in-repo via the official bootstrapper with v141 + WinXP optional components.

name: wxSQLite3 Windows v141_xp

on:
  workflow_dispatch:

env:
  WXSQLITE3_REPO: utelle/wxsqlite3
  # upstream default is main (not master); use a tag e.g. v4.12.2 for reproducible builds
  WXSQLITE3_REF: main
  WXWIDGETS_REF: v3.2.10
  # Side-by-side install path (avoids vswhere picking the wrong VS on the runner image)
  VS2022_INSTALL: C:\vs2022-buildtools

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout build repo
        uses: actions/checkout@v4

      - name: Checkout wxSQLite3
        uses: actions/checkout@v4
        with:
          repository: ${{ env.WXSQLITE3_REPO }}
          ref: ${{ env.WXSQLITE3_REF }}
          path: wxsqlite3

      - name: Install Visual Studio 2022 Build Tools (C++, MSVC v141, WinXP toolset pack)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $ProgressPreference = 'SilentlyContinue'
          $bootstrapper = Join-Path $env:TEMP "vs_BuildTools.exe"
          Invoke-WebRequest -Uri "https://aka.ms/vs/17/release/vs_BuildTools.exe" -OutFile $bootstrapper -UseBasicParsing
          $installPath = $env:VS2022_INSTALL
          Write-Host "Installing VS 2022 Build Tools to $installPath (first run can take 15-25 minutes)"
          $args = @(
            "--quiet", "--wait", "--norestart", "--nocache",
            "--installPath", $installPath,
            "--add", "Microsoft.VisualStudio.Workload.VCTools",
            "--add", "Microsoft.VisualStudio.Component.VC.v141.x86.x64",
            "--add", "Microsoft.VisualStudio.Component.WinXP"
          )
          $p = Start-Process -FilePath $bootstrapper -ArgumentList $args -Wait -PassThru -NoNewWindow
          if ($p.ExitCode -notin 0, 3010) {
            throw "vs_BuildTools.exe exited with $($p.ExitCode)"
          }
          $vswhere = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
          $verify = & $vswhere -latest -products Microsoft.VisualStudio.Product.BuildTools -property installationPath
          if ($verify -ne $installPath) {
            Write-Warning "vswhere BuildTools path is '$verify'; expected '$installPath'. Using VS2022_INSTALL for builds."
          }

      - name: Checkout wxWidgets
        uses: actions/checkout@v4
        with:
          repository: wxWidgets/wxWidgets
          ref: ${{ env.WXWIDGETS_REF }}
          path: wxWidgets

      - name: Write wx_local.props (vc_lib / vc_x64_lib layout)
        shell: pwsh
        run: |
          $xml = @'
          <?xml version="1.0" encoding="utf-8"?>
          <Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
            <PropertyGroup Label="UserMacros">
              <wxCompilerPrefix>vc</wxCompilerPrefix>
              <wxMsvcVersionAuto></wxMsvcVersionAuto>
            </PropertyGroup>
          </Project>
          '@
          $xml | Set-Content -Encoding utf8 -Path "$env:GITHUB_WORKSPACE/wxsqlite3/build/wx_local.props"

      - name: Build wxWidgets and wxSQLite3 (Win32 + x64, toolset v141_xp)
        shell: cmd
        run: |
          set VS_INST=%VS2022_INSTALL%
          if not exist "%VS_INST%\VC\Auxiliary\Build\vcvars32.bat" ( echo VS tools not found under %VS_INST% & exit /b 1 )
          set WXWIN=%GITHUB_WORKSPACE%\wxWidgets

          call "%VS_INST%\VC\Auxiliary\Build\vcvars32.bat"
          msbuild wxWidgets\build\msw\wx_vc16.sln /m /verbosity:minimal /p:Configuration=Release /p:Platform=Win32 /p:PlatformToolset=v141_xp
          if errorlevel 1 exit /b 1

          call "%VS_INST%\VC\Auxiliary\Build\vcvars64.bat"
          msbuild wxWidgets\build\msw\wx_vc16.sln /m /verbosity:minimal /p:Configuration=Release /p:Platform=x64 /p:PlatformToolset=v141_xp
          if errorlevel 1 exit /b 1

          call "%VS_INST%\VC\Auxiliary\Build\vcvars32.bat"
          msbuild wxsqlite3\build\wxsqlite3_vc15.sln /m /verbosity:minimal /t:wxsqlite3 /p:Configuration=Release /p:Platform=Win32 /p:PlatformToolset=v141_xp
          if errorlevel 1 exit /b 1

          call "%VS_INST%\VC\Auxiliary\Build\vcvars64.bat"
          msbuild wxsqlite3\build\wxsqlite3_vc15.sln /m /verbosity:minimal /t:wxsqlite3 /p:Configuration=Release /p:Platform=x64 /p:PlatformToolset=v141_xp
          if errorlevel 1 exit /b 1

      - name: Upload lib output
        uses: actions/upload-artifact@v4
        with:
          name: wxsqlite3-v141_xp-Release-static-lib
          path: |
            wxsqlite3/lib/**
